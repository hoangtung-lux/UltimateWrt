# Notice

UltimateWrt is an OpenWrt-based firmware profile.

It is not affiliated with or endorsed by the OpenWrt Project unless explicitly stated by upstream.

The project uses OpenWrt build tooling, package metadata, and target profiles. When distributing firmware images, keep the corresponding build scripts, package lists, overlays, patches and metadata available so users can inspect and rebuild the firmware.
